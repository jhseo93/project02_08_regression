# app.py (Streamlit 대시보드) 실행에 필요한 패키지
# scikit-learn 1.4 이상: root_mean_squared_error 사용
# streamlit 1.49 이상: st.dataframe(width="stretch"), st.bar_chart(horizontal=True) 사용
streamlit>=1.49
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4

# analysis/ 스크립트를 함께 실행하려면 아래도 필요
matplotlib>=3.8
